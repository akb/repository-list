#!/usr/bin/env bash

BASEPATH=$(cd "$(dirname "${BASH_SOURCE[0]}")"/.. && pwd)

cp $BASEPATH/node_modules/normalize.css/normalize.css \
   $BASEPATH/public/normalize.css

cp -R $BASEPATH/src/static/* $BASEPATH/public/
